CREATE VIEW DonorRecord AS
SELECT * FROM Donor;

CREATE VIEW DonationDetails AS
SELECT * FROM Donation;

CREATE VIEW DonorMedical AS
SELECT * FROM DonorScreening;

CREATE VIEW StaffRecord AS
SELECT * FROM Staff;

CREATE VIEW InventoryInfo AS
SELECT * FROM BloodInventory;

CREATE VIEW RecipientRecord AS
SELECT * FROM Recipient;

CREATE VIEW TransfusionDetails AS
SELECT * FROM Transfusion;


